import { Typography } from "@material-ui/core";

const Work = () => {
    return (
    <div style={{padding: "1rem"}}>
      <Typography variant="h4">work</Typography>
    </div>
    );
};
export default Work;
